{
  "name": "mges-data",
  "description": "Open Data for Museums, Galleries, and Exhibition Spaces in Japan: dataset, schema, and validation tooling",
  "private": true,
  "license": "CC0-1.0",
  "type": "module",
  "scripts": {
    "validate": "tsx scripts/validate.ts",
    "geocode": "tsx scripts/geocode.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "check": "pnpm typecheck && tsx scripts/geocode.ts --strict && pnpm validate && pnpm test"
  },
  "devDependencies": {
    "@geolonia/normalize-japanese-addresses": "^3.1.3",
    "@types/node": "^20.19.39",
    "ajv": "^8.17.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=20"
  }
}
